Class for console window for Linux OS was added. Implemented check for single instance of application running at same time. Solution works because a bind function return -1 when socket already was binded.
